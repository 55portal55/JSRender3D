(define (gamma color)
  color)
; (let
;   ((gma 1.8))
;   (let
;     ((r ((color 'get-x)))
;      (g ((color 'get-y)))
;      (b ((color 'get-z))))
;     (make-point
;       (expt r (/ 1.0 gma))
;       (expt g (/ 1.0 gma))
;       (expt b (/ 1.0 gma))))))
